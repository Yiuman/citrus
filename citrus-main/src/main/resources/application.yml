server:
  port: 8082
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/citrus?zeroDateTimeBehavior=convertToNull&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: yiuman

  activiti:
    db-history-used: true
    history-level: audit

citrus:
  jwt:
    token-validate-in-seconds: 60
